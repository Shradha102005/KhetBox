# Example environment variables for deployment (DO NOT commit real secrets)
# Copy this file to `.env` locally and fill in real values.
MONGO_URL=mongodb+srv://<DB_USER>:<PASSWORD>@<CLUSTER>.mongodb.net/<DB_NAME>?retryWrites=true&w=majority
DB_NAME=khetbox_production
CORS_ORIGINS=*
